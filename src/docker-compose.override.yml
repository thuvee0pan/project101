services:
  catalogDb:
    container_name: catalogDb
    environment:
      - POSTGRES_USER=postgras
      - POSTGRES_PASSWORD=postgras
      - POSTGRES_DB=CatalogDb
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - postgres_catalog:/var/lib/postgresql/data
    
  